# Core FastAPI dependencies (unpinned — install latest, then freeze)
# After `pip install -r requirements.txt`, run `pip freeze > requirements.lock`
# (or adopt uv / pyproject.toml) to pin exact versions for reproducible builds.
fastapi
uvicorn[standard]
python-dotenv
pydantic
pydantic-settings

# Optional: Add based on your needs (uncomment + install latest, then freeze)
# Database
# sqlalchemy
# asyncpg              # PostgreSQL async driver

# Authentication
# pyjwt                # JWT encode/decode (python-jose is unmaintained — prefer pyjwt)
# passlib[bcrypt]
# python-multipart

# HTTP Client
# httpx

# Testing
# pytest
# pytest-asyncio
# httpx                # For TestClient

# Development
# ruff
# mypy
